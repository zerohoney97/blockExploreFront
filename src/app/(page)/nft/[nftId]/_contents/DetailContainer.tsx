import React from "react";

const NftDetailContainer = ({ children }: { children: React.ReactNode }) => {
  return <div className="w-full h-auto  lg:flex">{children}</div>;
};

export default NftDetailContainer;
